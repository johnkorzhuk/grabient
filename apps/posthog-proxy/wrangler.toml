name = "posthog-proxy"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Development settings
[dev]
port = 8787

# Production environment
# After deploying, add a custom domain in Cloudflare dashboard
# Use something like: e.yourdomain.com or ph.yourdomain.com
# Avoid obvious names like: analytics, tracking, posthog, telemetry

# Routes (configure after deployment):
# routes = [
#   { pattern = "e.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
